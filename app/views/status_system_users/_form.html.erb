<%= form_for(@status_system_user) do |f| %>
  <% if @status_system_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_system_user.errors.count, "error") %> prohibited this status_system_user from being saved:</h2>

      <ul>
      <% @status_system_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
